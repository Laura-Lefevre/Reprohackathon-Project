Bootstrap: docker
From: ubuntu:20.04

%labels
    Author "Laura LEFEVRE"
    Description "Singularity container for mapping"

%post
    export DEBIAN_FRONTEND=noninteractive
    apt-get update
    apt-get install -y \
        wget \
        samtools \
        unzip \
        ca-certificates
    apt-get clean
    rm -rf /var/lib/apt/lists/*

    cd /opt
    
    wget https://sourceforge.net/projects/bowtie-bio/files/bowtie/0.12.7/bowtie-0.12.7-linux-x86_64.zip
    
    unzip bowtie-0.12.7-linux-x86_64.zip
    
    rm bowtie-0.12.7-linux-x86_64.zip

%environment
    export PATH="/opt/bowtie-0.12.7:$PATH"

%help
    Container for Reprohackathon (mapping).
    Contains: wget, samtools, et Bowtie v0.12.7.
